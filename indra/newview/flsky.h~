/**
* Dunno what to license this yet.  Assume closed-source (AKA if you're not me, close this window kthx)
*/
#ifndef FL_SKY_H
#define FL_SKY_H

class FLSky
{
public:
	// @brief Sets up the class.
	static void Init();

	// @brief Get instance.
	//static FLSky* 		getInstance();

	// @brief Render clouds.
	static void Render();

private:
	static VolumetricClouds*	mClouds;
	
};
#endif // FL_SKY_H
